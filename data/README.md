# Data Layout

This repository keeps the `data/` directory as a placeholder only.

Expected local dataset mounts:

```text
data/
├── TJ4D/
└── VoD/
```

The real dataset contents are not committed to GitHub. In the local machine
used for training and evaluation, these paths may be symlinks or mounted
directories that point to external storage.

